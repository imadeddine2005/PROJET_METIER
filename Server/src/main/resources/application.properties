spring.application.name=login_test
# Configuration MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/Smart_recruite
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=false

# Port (déjà configuré chez toi)
server.port=8090

jwt.secret=${JWT_SECRET}
jwt.expiration=3600000

# Stockage des fichiers CV
app.upload.cv-dir=uploads/cvs

# Taille maximale des fichiers (5 MB)
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# === SERVICE IA (Flask) ===
# En développement : app1.py tourne sur port 5001
app.ai.base-url=http://localhost:5001

# === MAIL CONFIGURATION (Gmail) ===
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# Remplace par ton vrai email Gmail (ex: ton.nom@gmail.com)
spring.mail.username=okratiimadyt@gmail.com
# Remplace par ton Mot de passe d'application généré sur Google (16 lettres, pas le mdp normal)
spring.mail.password=apwi tqih tvaj byfd
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

